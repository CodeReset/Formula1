import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  copyRightText: {
    fontSize: 17,
    fontFamily: 'Formula1-Regular',
    color: '#fff',
    textAlign: 'center',
  },
});
